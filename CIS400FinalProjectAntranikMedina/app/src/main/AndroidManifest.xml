<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cis400finalprojectantranikmedina">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BetInput"></activity>
        <activity android:name=".BettingHistory" />
        <activity android:name=".LiveBets" />
        <activity
            android:name=".LearnBetting"
            android:label="@string/title_activity_learn_betting"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CreateUser" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateBet" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>